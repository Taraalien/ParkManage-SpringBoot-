server:
  port: 8090

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/parkmange
    username: postgres
    password: 123


  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    database: POSTGRESQL
    hibernate:
      ddl-auto: update
debug: true
